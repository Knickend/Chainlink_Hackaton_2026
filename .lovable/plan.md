

# Change On-Chain Status Badge to Green

## Change

**`src/components/ExchangeRatesDialog.tsx`** (line 30): Update the `on-chain` variant in the `StatusBadge` component from `bg-primary/20 text-primary border-primary/30` to `bg-success/20 text-success border-success/30` so successfully fetched on-chain feeds display in green, matching the "Live" badge style and clearly indicating success.

## File modified

| File | Change |
|------|--------|
| `src/components/ExchangeRatesDialog.tsx` | Change on-chain badge color from primary (gold/amber) to success (green) |

