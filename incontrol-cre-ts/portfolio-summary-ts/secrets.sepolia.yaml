# Secrets for publishing to Sepolia
# DO NOT commit real secrets to public repos. This file is a local stub.
# Replace placeholders below with your actual values or provide via environment
# variables and set `secrets-path` accordingly.
WALLET_PRIVATE_KEY: "<YOUR_SEPOLIA_PRIVATE_KEY>" # 0x...
RPC_URL: "<YOUR_SEPOLIA_RPC_URL>"               # e.g. https://sepolia.infura.io/v3/<key>
EXCHANGE_API_KEY: ""                             # If your exchange requires a key
